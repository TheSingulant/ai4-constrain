# Constraint protocol (pointer)

The frozen pilot protocol is `docs/protocol-v0.1.md`.
The frozen benchmark protocol is `docs/benchmark-protocol-v0.1-frozen.md`.
This file is a leftover scaffold name, not a third spec.
