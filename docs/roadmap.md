# Roadmap (docs pointer)

Canonical public roadmap: [`../ROADMAP.md`](../ROADMAP.md).

**0.7.0 (done):** optional hybrid governing integration (default OFF), semantic packaged artifacts, public CI/docs.

**Next:** dedicated-context lifecycle and expanded authority-control work (not claimed complete here). Transaction-control scaffold (`ai4.transaction`) is branch work only and is not a 0.7.0 completeness claim. The DevNet E2E proof harness is the same class of unreleased work.

Production bot/host work stays off this path. Stage 2D remains `null_retained_D_adds_cost`.
